"use strict";

// didnot use var
somthing = "hello world"; // use var

// duplicate parameters
var person={
    u_name:1,
    u_name:2 // not use same parameter
};

// function parameter error

function myFunction(a,b,a)
{
    return a + b + c; 
}

// output data
document.getElementById('heading').innerHTML = somthing;